{
    "$schema": "https://openapi.vercel.sh/vercel.json",
    "installCommand": "curl -fsSL https://bun.sh/install | bash && export BUN_INSTALL=\"$HOME/.bun\" && export PATH=$BUN_INSTALL/bin:$PATH && ~/.bun/bin/bun install --frozen-lockfile",
    "buildCommand": "~/.bun/bin/bun run build"
}